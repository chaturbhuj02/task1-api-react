import LoadTable from "./LoadTable";
import classes from "./CreateTable.module.css";

const CreateTable = () => {
  return (
    <div>
      Build a table to load data
      <div>
        <LoadTable />
      </div>
    </div>
  );
};

export default CreateTable;
